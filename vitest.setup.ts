// Roda uma vez antes de todos os testes.
// `@testing-library/jest-dom` adiciona "matchers" amigáveis ao expect, como
// toBeInTheDocument(), toHaveTextContent(), toBeVisible() — usados nos testes de componente.
import "@testing-library/jest-dom";
